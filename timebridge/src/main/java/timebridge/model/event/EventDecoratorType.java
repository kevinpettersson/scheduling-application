package timebridge.model.event;

/**
 * Represents different types of event decorators which are used to add additional information to an {@link Event}.
 */
public enum EventDecoratorType {
    COURSE,
    ACTIVITY,
    LOCALE,
}
